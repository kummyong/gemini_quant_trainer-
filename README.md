# Gemini Quant - Server Trainer

This directory is set up as the training server workspace for the Gemini Quant project.
Here, we will run the `trainer.py` script on the PC (Training Server) to optimize hyperparameters and send them to the Edge Inference device (Mobile).
